[package]
name = "api-key-checker-tauri"
version = "0.1.5"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
rust-helper = { path = "../rust-helper" }
tauri = { version = "2", features = [] }
